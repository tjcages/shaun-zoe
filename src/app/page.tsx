import { Canvas } from "@/components";

export default function Home() {
  return (
    <main className="fixed top-0 left-0 right-0 bottom-0 flex min-h-screen flex-col items-center justify-between p-24">
      <Canvas />
    </main>
  );
}
